export const Tecla = (props) => {
  return (
    <button
      style={{
        background: "black",
        color: "white",
        border: "none",
        padding: 8,
        cursor: "pointer",
      }}
      {...props}
    />
  );
};
